import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
  aboutCard: {
    borderBottomColor: 'lightgrey',
    borderBottomWidth: 1,
    paddingBottom: 15,
    marginBottom: 10
  },
});
